/* crosswordCompiler.js */
// Funzioni principali per il rendering e l'interazione dei cruciverba
function CrosswordCompiler(puzzleData, options, config) {
    // Inizializzazione
    console.log("CrosswordCompiler inizializzato", puzzleData);
}